{
  "name": "phys-sim-capstone",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && node rename-html.js",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.1"
  },
  "dependencies": {
    "ts-matrix": "^1.3.2"
  }
}
